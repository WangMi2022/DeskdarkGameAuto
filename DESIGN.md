# 桌面助手界面记录

作用范围：`renderer-helper.js` 内注入的浮动面板。保留既有深色界面，扩展金币材料、爬塔和免费领奖入口。

- 面板与游戏采用 Flexbox 水平双列隔离布局：左侧游戏容器（#root）自适应填充全部可用空间并开启独立滚动与 paint 隔离；右侧助手面板固定 400 像素独立列，通过全高边框完全隔离在游戏界面外，二者互为平级兄弟节点，100% 杜绝任何遮挡或重叠。当关闭面板时，游戏容器自动扩满 100% 窗口。
- 使用系统中文字体和浅色正文。背景为深蓝灰 `#131a24`，表单与列表底色为 `#0f151e`，边界为 `#344255`；开始按钮为浅绿，停止按钮为暗红。
- 五个入口紧凑排列：自动强化、金币材料、世界 BOSS、自动爬塔、自动领奖。每页保留独立开始操作与停止按钮；任何任务进行中锁定其他开始按钮、参数和刷新按钮。
- 金币材料页显示材料名称、实际购买个数、份数、金币总价，并提供三个单项按钮和“一键购买三种”。无有效订单时显示原因并禁用购买。
- 爬塔页显示最高通关与下一层、目标层数、间隔、默认使用技能，以及胜利继续、失败停止的规则。
- 领奖页显示公会和活动可领取数量，支持分别领取或一键领取；只处理服务器可领取标记（包括推币场已解锁奖励），不执行消费类操作。
- 所有数值输入有可见标签，按钮保留键盘焦点提示。停止为所有任务共用动作；关闭面板同样停止后续操作，菜单可重新打开。
- 运行日志使用文本与颜色共同区分结果。截图验证范围为 1440×920 和 1000×680 的桌面窗口；截图为离线模拟，不含真实账号数据。

该文件记录现有实现，不定义新的品牌或视觉体系。
